name: futbolcu_tahmin_etmek
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.4
  audioplayers: ^6.1.0
  firebase_core: ^4.4.0
  cloud_firestore: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  http: ^1.6.0
  html: ^0.15.6

flutter:
  uses-material-design: true

  # Tüm asset klasörlerini burada tek bir listede topluyoruz:
  assets:
    - assets/flags/
    - assets/backgrounds/
    - assets/players/
    - assets/data/
    - assets/music/
    - assets/images/